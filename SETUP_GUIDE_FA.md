# راهنمای جامع راه‌اندازی سیستم SecureRedLab v2.0 (نسخه ایجنتیک)

<div dir="rtl">

سلام داداش! این راهنمای کامل برای اجرای سیستم جدید روی سیستم خودته. همه چیز رو قدم به قدم و به زبان ساده نوشتم.

---

## 💻 ۱. سخت‌افزار مورد نیاز (Hardware Requirements)

برای اجرای مدل‌های هوش مصنوعی به صورت **آفلاین**، نیاز به سخت‌افزار مناسب داری.

### حداقل سیستم (برای اجرای مدل‌های کوچک):
- **RAM**: حداقل 16 گیگابایت
- **GPU**: کارت گرافیک NVIDIA با حداقل 8 گیگابایت VRAM (مثل RTX 3060)
- **CPU**: پردازنده 6 هسته‌ای (مثل Core i5 یا Ryzen 5)
- **Storage**: 50 گیگابایت فضای خالی (SSD توصیه می‌شود)

### سیستم پیشنهادی (برای اجرای روان DeepSeek-R1 و Nemotron):
- **RAM**: 32 یا 64 گیگابایت
- **GPU**: کارت گرافیک NVIDIA با 16 یا 24 گیگابایت VRAM (مثل RTX 3090 یا RTX 4090)
- **Storage**: 200 گیگابایت فضای خالی روی NVMe SSD

---

## 🛠️ ۲. پیش‌نیازهای نرم‌افزاری

قبل از شروع، این موارد رو نصب کن:
1. **Python 3.11**: حتماً نسخه 3.11 باشه.
2. **Node.js 20+**: برای اجرای فرانت‌اِند.
3. **Redis**: برای مدیریت پیام‌های Real-time.
4. **NVIDIA Drivers & CUDA**: اگر کارت گرافیک NVIDIA داری، حتماً آخرین درایور و CUDA Toolkit رو نصب کن.

---

## 🚀 ۳. مراحل راه‌اندازی (Step-by-Step)

### مرحله اول: آماده‌سازی محیط پایتون
```bash
cd SecureRedLab_v2_Full/backend
python -m venv venv
source venv/bin/activate  # در ویندوز: venv\Scripts\activate
pip install -r requirements.txt
```

### مرحله دوم: راه‌اندازی سرور مدل‌های هوش مصنوعی (vLLM)
ما از `vLLM` برای اجرای آفلاین مدل‌ها استفاده می‌کنیم. در یک ترمینال جداگانه:
```bash
# اجرای مدل اصلی تصمیم‌گیری (DeepSeek-R1)
vllm serve deepseek-ai/DeepSeek-R1 --port 8000
```
*نکته: اگر کارت گرافیکت ضعیفه، می‌تونی از نسخه‌های Quantized شده (مثل GGUF) با استفاده از LM Studio یا Ollama استفاده کنی و آدرس API رو در فایل `config.py` تغییر بدی.*

### مرحله سوم: اجرای بک‌اِند (Backend)
در ترمینال اصلی (جایی که venv فعاله):
```bash
cd backend
python main.py
```
بک‌اِند روی پورت **8000** اجرا می‌شه.

### مرحله چهارم: اجرای فرانت‌اِند (Frontend)
یک ترمینال جدید باز کن:
```bash
cd SecureRedLab_v2_Full/frontend
npm install
npm run dev
```
فرانت‌اِند روی پورت **5173** (یا مشابه) اجرا می‌شه.

---

## 🖥️ ۴. نحوه تست و مشاهده سیستم ایجنتیک

1. مرورگر رو باز کن و به آدرس `http://localhost:5173` برو.
2. در منوی سمت چپ، گزینه جدید **"Agentic AI"** رو می‌بینی. روش کلیک کن.
3. در صفحه باز شده:
   - در قسمت **Target**، آدرس IP یک سیستم تست (مثلاً `127.0.0.1`) رو وارد کن.
   - در قسمت **Goal**، هدفت رو بنویس (مثلاً: "اسکن پورت‌های باز و شناسایی سرویس‌های آسیب‌پذیر").
   - دکمه **"شروع عملیات هوشمند"** رو بزن.
4. حالا می‌تونی در قسمت پایین صفحه (Terminal)، لاگ‌های واقعی ارتباط بین Agents رو ببینی که چطور با هم حرف می‌زنن و نقشه می‌کشن.

---

## ❓ سوالات متداول

**آیا واقعاً آفلاینه؟**
بله داداش! اگر مدل‌ها رو دانلود کرده باشی، سیستم هیچ نیازی به اینترنت نداره و تمام پردازش‌ها روی کارت گرافیک خودت انجام می‌شه.

**اگر کارت گرافیک نداشته باشم چی؟**
می‌تونی از نسخه CPU مدل‌ها استفاده کنی (با Ollama)، اما سرعتش خیلی پایین‌تره. برای ارائه دانشگاهی، پیشنهاد می‌کنم حتماً روی یک سیستم دارای GPU تست کنی.

**چطور بفهمم Agents دارن کار می‌کنن؟**
در کنسول بک‌اِند (ترمینال پایتون)، لاگ‌های بنفش و آبی رنگی می‌بینی که با پیشوند `[Decision Agent]` یا `[Execution Agent]` شروع می‌شن. این یعنی مغز سیستم فعاله!

---

داداش، این سیستم الان یک **غول ایجنتیک** واقعی شده. با خیال راحت برو برای ارائه! 🎓🚀
</div>
